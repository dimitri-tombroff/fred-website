# fred-website

This is a hugo static site generated using hugo and doks template for the Fred open source project. 
To generate the documentation do the following:

```sh
npm install
````

Then :

```sh
npn run dev
```
